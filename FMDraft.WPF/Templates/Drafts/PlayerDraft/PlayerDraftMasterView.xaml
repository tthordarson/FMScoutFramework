<UserControl x:Class="FMDraft.WPF.Templates.Drafts.PlayerDraftViewModek.PlayerDraftMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FMDraft.WPF.Templates.Drafts.PlayerDraft"
             xmlns:team="clr-namespace:FMDraft.WPF.Templates.Team"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:PlayerDraftRoundViewModel}">
            <local:PlayerDraftRoundView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type team:TeamViewModel}">
            <team:PlayersView />
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>  
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        
        <Label 
            Grid.Row="0" 
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Grid.RowSpan="2"
            Content="{Binding DraftPanel}"
            Foreground="{Binding DraftPanelForegroundColor}"
            Background="{Binding DraftPanelBackgroundColor}"
            Margin="10" />
        
        <Button Grid.Row="0"
                Grid.Column="2">Draft Player</Button>
        
        <Button
            Grid.Row="1"
            Grid.Column="2">Next Pick</Button>

        <ContentControl Grid.Row="2" Grid.Column="0" DataContext="{Binding SelectedDraftRound}" />
        
        <local:AvailablePlayersView Grid.Row="2" Grid.Column="1" DataContext="{Binding AvailablePlayers}" />
        
        <Grid Grid.Row="2" Grid.Column="3" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <ComboBox Grid.Row="0" ItemsSource="{Binding Teams}" SelectedItem="{Binding SelectedTeam}" />
            <ContentControl Grid.Row="1" Content="{Binding SelectedTeam}" />
        </Grid>
        
    </Grid>
</UserControl>
